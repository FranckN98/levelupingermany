import AdminConsole from '@/components/AdminConsole';

export const metadata = { title: 'Administration | Eddy Gaetan', robots: { index: false, follow: false } };

export default function AdminPage() {
  return <AdminConsole />;
}